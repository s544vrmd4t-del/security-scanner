{
  "name": "code-review-bot",
  "version": "1.0.0",
  "description": "AI-powered code review GitHub Action, DeepSeek-driven",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --target=node20 --outfile=dist/index.js",
    "dev": "node index.js"
  },
  "keywords": ["github-action", "code-review", "ai", "deepseek"],
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
